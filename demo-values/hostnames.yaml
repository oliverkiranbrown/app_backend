elementAdmin:
  ingress:
    host: admin.murmr.localhost
elementWeb:
  ingress:
    host: chat.murmr.localhost
matrixAuthenticationService:
  ingress:
    host: auth.murmr.localhost
matrixRTC:
  ingress:
    host: mrtc.murmr.localhost
serverName: murmr.localhost
synapse:
  ingress:
    host: matrix.murmr.localhost
